apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: podinfo
  namespace: flux-system
spec:
  suspend: true
  imageRepositoryRef:
    name: java-sb
  policy:
    semver:
      range: 0.0.x
